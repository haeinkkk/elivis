ignore-workspace-root-check=true

# Windows: Prisma "EPERM rename query_engine-windows.dll.node" under node_modules/.pnpm
# (Defender / indexing / OneDrive). Hoisted layout matches npm and usually avoids it.
node-linker=hoisted
